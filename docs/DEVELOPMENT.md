# 开发指南

## 🚀 快速开始

### 1. 安装依赖
```bash
pnpm install
```

### 2. 启动开发服务器
```bash
pnpm dev
```

浏览器会自动打开 `http://localhost:5173`

### 3. 常用命令

```bash
# 开发
pnpm dev              # 启动开发服务器

# 构建
pnpm build            # 构建生产版本
pnpm preview          # 预览生产版本

# 代码质量
pnpm lint             # ESLint 检查
pnpm format           # Prettier 格式化
```

---

## 📁 项目结构

```
src/
├── components/        # 可复用组件
│   └── ComponentRenderer.vue
├── views/            # 页面视图
│   ├── Home.vue      # 项目首页
│   ├── Designer.vue  # 设计器
│   └── Preview.vue   # 预览页面
├── stores/           # Pinia 状态管理
│   └── project.ts
├── services/         # API 服务
│   └── projectService.ts
├── router/           # 路由配置
│   └── index.ts
├── utils/            # 工具函数
│   └── api.ts
├── types/            # TypeScript 类型
│   └── index.ts
├── config/           # 配置文件
│   └── uiFrameworks.ts
├── style/            # 全局样式
│   └── main.scss
├── App.vue           # 根组件
└── main.ts           # 入口文件
```

---

## 🔧 开发规范

### 代码风格
- 使用 `<script setup>` 语法编写 Vue 组件
- TypeScript 类型检查
- 遵循 ESLint 规则
- Prettier 自动格式化

### 命名规范
- 组件: PascalCase (如 ComponentRenderer)
- 文件: kebab-case (如 component-renderer.vue)
- 变量: camelCase (如 projectStore)
- 常量: UPPER_CASE (如 API_BASE_URL)

### 文件组织
- 相关文件放在同一目录
- 使用 @ 别名导入
- 避免循环依赖

---

## 📝 开发检查清单

### 初始化步骤
- [ ] 运行 `pnpm install` 安装依赖
- [ ] 创建 `.env.local` 文件
- [ ] 配置 API 服务器地址
- [ ] 运行 `pnpm dev` 启动开发服务器

### 代码规范
- [ ] 运行 `pnpm lint` 检查代码
- [ ] 运行 `pnpm format` 格式化代码
- [ ] 没有 ESLint 警告
- [ ] TypeScript 类型检查通过

### 功能测试
- [ ] 项目创建功能正常
- [ ] UI 框架选择正常
- [ ] 设计器基础功能正常
- [ ] 预览功能正常
- [ ] 路由导航正常

### 构建验证
- [ ] `pnpm build` 成功
- [ ] 没有构建错误
- [ ] dist 目录生成正确

---

## 🎯 开发计划

### 第一阶段: 基础搭建 ✅ (已完成)
- [x] 项目初始化
- [x] 框架集成
- [x] 基础页面
- [x] 状态管理
- [x] API 客户端

### 第二阶段: 核心功能 ⏳ (待开始)
- [ ] 组件拖拽系统
- [ ] 属性编辑面板
- [ ] 组件树管理
- [ ] 代码生成引擎

### 第三阶段: AI 功能 ⏳ (待开始)
- [ ] 图片上传
- [ ] AI 图像识别
- [ ] 自动代码生成

### 第四阶段: 发布功能 ⏳ (待开始)
- [ ] 项目打包
- [ ] 项目部署
- [ ] 发布管理

---

## 🔌 API 集成

项目已集成 Axios API 客户端，支持：
- 请求/响应拦截器
- 自动 Token 管理
- 错误处理

### 使用示例

```typescript
import apiClient from '@/utils/api'

// GET 请求
const data = await apiClient.get('/api/projects')

// POST 请求
const result = await apiClient.post('/api/projects', { 
  name: 'My Project' 
})

// PUT 请求
await apiClient.put('/api/projects/1', { name: 'Updated' })

// DELETE 请求
await apiClient.delete('/api/projects/1')
```

---

## 🐛 常见问题

### Q: 如何修改 API 服务器地址？
A: 编辑 `.env.local` 文件，修改 `VITE_API_BASE_URL` 变量。

### Q: 如何切换 UI 框架？
A: 在创建项目时选择不同的 UI 框架，后续会自动加载对应的组件库。

### Q: 如何调试应用？
A: 使用 Vue DevTools 浏览器扩展调试组件和状态。

### Q: 构建失败怎么办？
A: 清除 node_modules 和 pnpm-lock.yaml，重新运行 `pnpm install`。

---

## 🔧 开发工具

### IDE
- **VS Code** (推荐)
- **WebStorm**

### 浏览器扩展
- **Vue DevTools** (必需)
- **Chrome DevTools**

### 开发依赖
- **Node.js** >= 16
- **pnpm** >= 7.33.4

---

## 📞 获取帮助

1. 查看 README.md 了解项目概览
2. 查看 ARCHITECTURE.md 了解系统架构
3. 查看本文件了解开发指南
4. 检查浏览器控制台错误信息
5. 使用 Vue DevTools 调试

---

**最后更新**: 2025-11-28
