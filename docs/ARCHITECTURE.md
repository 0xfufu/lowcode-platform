# 项目架构文档

## 系统架构概览

```
┌─────────────────────────────────────────────────────────────�?�?                   低代码平台前端应�?                         �?├─────────────────────────────────────────────────────────────�?�?                                                              �?�? ┌──────────────�? ┌──────────────�? ┌──────────────�?      �?�? �?  Home.vue   �? �?Designer.vue �? �?Preview.vue  �?      �?�? �? 项目管理     �? �?  设计�?     �? �?  预览页面    �?      �?�? └──────────────�? └──────────────�? └──────────────�?      �?�?        �?                 �?                 �?             �?�?        └──────────────────┼──────────────────�?             �?�?                           �?                                �?�? ┌────────────────────────────────────────────────────�?    �?�? �?          Vue Router (路由管理)                      �?    �?�? └────────────────────────────────────────────────────�?    �?�?                           �?                                �?�? ┌────────────────────────────────────────────────────�?    �?�? �?     Pinia Store (状态管�?                          �?    �?�? �? ┌──────────────────────────────────────────────�? �?    �?�? �? �? Project Store                               �? �?    �?�? �? �? - projects: Project[]                       �? �?    �?�? �? �? - currentProject: Project | null            �? �?    �?�? �? �? - actions: add/update/delete/setCurrent     �? �?    �?�? �? └──────────────────────────────────────────────�? �?    �?�? └────────────────────────────────────────────────────�?    �?�?                           �?                                �?�? ┌────────────────────────────────────────────────────�?    �?�? �?     Services & Utils (业务逻辑)                     �?    �?�? �? ┌──────────────────────────────────────────────�? �?    �?�? �? �? projectService.ts                           �? �?    �?�? �? �? - getProjects()                             �? �?    �?�? �? �? - createProject()                           �? �?    �?�? �? �? - updateProject()                           �? �?    �?�? �? �? - deleteProject()                           �? �?    �?�? �? �? - publishProject()                          �? �?    �?�? �? └──────────────────────────────────────────────�? �?    �?�? �? ┌──────────────────────────────────────────────�? �?    �?�? �? �? api.ts (Axios 客户�?                        �? �?    �?�? �? �? - 请求拦截�?                               �? �?    �?�? �? �? - 响应拦截�?                               �? �?    �?�? �? �? - Token 管理                                �? �?    �?�? �? └──────────────────────────────────────────────�? �?    �?�? └────────────────────────────────────────────────────�?    �?�?                           �?                                �?�? ┌────────────────────────────────────────────────────�?    �?�? �?     Axios HTTP Client                            �?    �?�? └────────────────────────────────────────────────────�?    �?�?                           �?                                �?└────────────────────────────┼────────────────────────────────�?                             �?                    ┌────────▼────────�?                    �? 后端 API 服务   �?                    �?(Node.js/Python) �?                    └─────────────────�?```

## 数据流向

### 1. 项目创建流程

```
用户输入 �?Home.vue 表单 �?Pinia Store �?projectService.post() 
�?Axios �?后端 API �?数据�?�?响应 �?Store 更新 �?UI 更新
```

### 2. 设计编辑流程

```
用户拖拽 �?Designer.vue �?Pinia Store 更新 �?组件树变�?
�?ComponentRenderer 重新渲染 �?画布更新
```

### 3. 代码生成流程

```
设计完成 �?收集组件�?�?代码生成引擎 �?生成 Vue 代码 
�?导出或发�?�?后端保存/部署
```

## 核心模块详解

### 1. 路由模块 (src/router/index.ts)

```typescript
Routes:
├── / (Home) - 项目首页
├── /designer - 设计�?└── /preview/:id - 预览页面
```

**特点**:
- 使用懒加载加速初始加�?- 支持动态路由参�?- 集成 Vue Router 4

### 2. 状态管�?(src/stores/project.ts)

```typescript
State:
├── projects: Project[] - 项目列表
└── currentProject: Project | null - 当前编辑项目

Actions:
├── addProject(project) - 添加项目
├── updateProject(project) - 更新项目
├── deleteProject(id) - 删除项目
└── setCurrentProject(project) - 设置当前项目
```

**特点**:
- 使用 Composition API 风格
- 支持异步操作
- 自动状态持久化（可扩展�?
### 3. API 客户�?(src/utils/api.ts)

```typescript
Features:
├── 请求拦截�?- 自动添加 Token
├── 响应拦截�?- 统一错误处理
├── 自动重试机制（可扩展�?└── 超时控制
```

**使用示例**:
```typescript
const data = await apiClient.get('/api/projects')
const result = await apiClient.post('/api/projects', payload)
```

### 4. 服务�?(src/services/projectService.ts)

```typescript
Methods:
├── getProjects() - 获取项目列表
├── getProject(id) - 获取单个项目
├── createProject(data) - 创建项目
├── updateProject(id, data) - 更新项目
├── deleteProject(id) - 删除项目
├── exportProject(id) - 导出项目
└── publishProject(id) - 发布项目
```

**特点**:
- 封装 API 调用逻辑
- 提供统一的数据接�?- 便于单元测试

### 5. UI 框架配置 (src/config/uiFrameworks.ts)

```typescript
Frameworks:
├── Element Plus
�?  └── components: Button, Input, Table, Form, Card, Text
├── Ant Design Vue
�?  └── components: Button, Input, Table, Form, Card, Text
└── Naive UI
    └── components: Button, Input, Table, Form, Card, Text
```

**扩展方式**:
```typescript
// 添加新框�?{
  name: 'Vuetify',
  value: 'vuetify',
  components: [...]
}
```

## 组件结构

### Home.vue - 项目管理

```
┌─────────────────────────────────────�?�?        Header (标题�?              �?�? ┌─────────────────────────────────┐│
�? �?低代码平�?     [新建项目]       ││
�? └─────────────────────────────────┘│
├─────────────────────────────────────�?�?        Main (主内容区)              �?�? ┌──────────�?┌──────────�?┌──────�?�?�? �?项目卡片 �?�?项目卡片 �?�?...  �?�?�? └──────────�?└──────────�?└──────�?�?├─────────────────────────────────────�?�?        Dialog (新建项目对话�?       �?�? ┌─────────────────────────────────┐│
�? �?项目名称: [输入框]               ││
�? �?项目描述: [文本框]               ││
�? �?UI框架:  [下拉框]                ││
�? �?        [取消] [确定]            ││
�? └─────────────────────────────────┘│
└─────────────────────────────────────�?```

### Designer.vue - 设计�?
```
┌──────────────────────────────────────────────────────────�?�? Header: [返回] 项目名称 [预览] [发布]                    �?├──────────────────────────────────────────────────────────�?�?Sidebar  �?          Canvas          �?  Properties     �?�?         �?                          �?                 �?�?页面管理 �?  ┌─────────────────────�?�?属性面�?        �?�?┌──────�?�?  �?                    �?�?┌──────────────�?�?�?│页�? �?�?  �?  拖拽组件到此�?   �?�?�?选择组件查看  �?�?�?└──────�?�?  �?                    �?�?�?属�?         �?�?�?         �?  └─────────────────────�?�?└──────────────�?�?�?组件�?  �?                          �?                 �?�?┌──────�?�?                          �?                 �?�?│按�? �?�?                          �?                 �?�?├──────�?�?                          �?                 �?�?│输入框�?�?                          �?                 �?�?├──────�?�?                          �?                 �?�?│表�? �?�?                          �?                 �?�?└──────�?�?                          �?                 �?└──────────────────────────────────────────────────────────�?```

### Preview.vue - 预览页面

```
┌──────────────────────────────────────�?�? Header: [返回] 预览                  �?├──────────────────────────────────────�?�?                                     �?�?        预览内容区域                  �?�?                                     �?�? (显示设计的页面效�?                 �?�?                                     �?└──────────────────────────────────────�?```

## 数据模型

### Project (项目)

```typescript
interface Project {
    id: string                    // 唯一标识
    name: string                  // 项目名称
    description: string           // 项目描述
    pages: Page[]                 // 页面列表
    uiFramework: string           // UI框架选择
    createdAt: string             // 创建时间
    updatedAt: string             // 更新时间
}
```

### Page (页面)

```typescript
interface Page {
    id: string                    // 唯一标识
    name: string                  // 页面名称
    components: Component[]       // 组件列表
}
```

### Component (组件)

```typescript
interface Component {
    id: string                    // 唯一标识
    type: string                  // 组件类型
    props: Record<string, any>    // 组件属�?    children?: Component[]        // 子组�?}
```

## 技术栈详解

| 技�?| 版本 | 用�?|
|------|------|------|
| Vue | 3.4+ | 前端框架 |
| TypeScript | 5.3+ | 类型检�?|
| Vite | 5.0+ | 构建工具 |
| Pinia | 2.1+ | 状态管�?|
| Vue Router | 4.3+ | 路由管理 |
| Axios | 1.6+ | HTTP 客户�?|
| Element Plus | 2.5+ | UI 组件�?|
| SCSS | 1.69+ | 样式预处�?|
| ESLint | 8.55+ | 代码检�?|
| Prettier | 3.1+ | 代码格式�?|

## 扩展�?
### 1. 添加新的 UI 框架

```typescript
// 1. �?src/config/uiFrameworks.ts 中添加框架配�?// 2. �?main.ts 中条件加载框�?// 3. �?ComponentRenderer.vue 中添加映�?```

### 2. 添加新的组件类型

```typescript
// 1. �?uiFrameworks.ts 中添加组件定�?// 2. �?ComponentRenderer.vue 中添加渲染逻辑
// 3. �?Designer.vue 中添加组件库�?```

### 3. 集成 AI 功能

```typescript
// 1. 创建 src/services/aiService.ts
// 2. 实现图片上传和识�?// 3. 实现代码生成逻辑
// 4. �?Designer.vue 中集�?AI 按钮
```

### 4. 实现代码生成

```typescript
// 1. 创建 src/services/codeGeneratorService.ts
// 2. 实现 Vue 代码生成模板
// 3. 实现样式生成逻辑
// 4. 实现导出功能
```

## 性能优化建议

1. **组件懒加�?* - 使用 Vue Router 的懒加载
2. **代码分割** - 按需加载 UI 框架
3. **虚拟滚动** - 大列表使用虚拟滚�?4. **缓存策略** - 缓存项目数据�?API 响应
5. **图片优化** - 压缩和懒加载设计图片

## 安全考虑

1. **Token 管理** - 使用 localStorage 存储 Token
2. **请求验证** - 验证所有用户输�?3. **CORS 配置** - 配置正确�?CORS 策略
4. **XSS 防护** - Vue 自动转义模板内容
5. **CSRF 防护** - 使用 Token 验证请求

## 测试策略

1. **单元测试** - 测试 Store �?Service
2. **组件测试** - 测试 Vue 组件
3. **集成测试** - 测试完整流程
4. **E2E 测试** - 使用 Playwright 测试用户流程

## 部署指南

1. **构建**: `pnpm build`
2. **输出**: `dist/` 目录
3. **部署**: 上传�?Web 服务�?4. **配置**: 设置 API 服务器地址

---

**更新日期**: 2025-11-28
